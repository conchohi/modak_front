import React from "react";

// CampingFinder 컴포넌트 정의
const CampingFinder = ({ filteredCampings }) => {
  // 여기에서 props를 통해 filteredCampings를 받습니다.
  return <div>{/* filteredCampings를 사용한 로직 */}</div>;
};

export default CampingFinder; // 컴포넌트를 정상적으로 export 합니다.
